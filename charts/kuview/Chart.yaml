apiVersion: v2
name: kuview
description: A Helm chart for deploying KuView, a browser-based, real-time Kubernetes dashboard.
type: application
version: 0.1.0
appVersion: "latest" # Or a specific version if known
